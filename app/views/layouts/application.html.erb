<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Rails Recurly.js Sample Application</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <%= yield :head %>
    <%= include_gon %>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <ul class="nav nav-pills pull-right">
          <% unless current_user %>
          <li><%= link_to "Sign in", dashboard_path %></li>
          <% end %>
          <% if current_user && controller.controller_name != 'subscriptions' %>
          <li><%= link_to("Sign out", destroy_user_session_path, :method => :delete) %></li>
          <% end %>
        </ul>
        <h3 class="text-muted"><%= link_to 'SuperSaaS', '/' %></h3>
      </div>
      <%= render partial: 'shared/notice' %>
      <%= yield %>
      <div class="footer" style="margin-top: 3em;">
        <p>&copy; Acme Corp 2013</p>
      </div>
    </div>
  </body>
</html>